<h1>New Club</h1>
<%= form_with scope: :club, url: clubs_path, local: true do |form| %>
	<% if @club.errors.any? %>
		<div id="error_explanation">
		<h2>
			<%= pluralize(@article.errors.count, "error") %> prohibited this club from being created
		</h2>
		<ul>
			<% @club.errors.full_messages.each do |msg %>
				<li><%- msg %></li>
			<% end %>
		</ul>
	</div>
	<% end %>
	<p>
		<%= form.label :name %><br>
		<%= form.text_field :name %><br>
	</p>

	<p>
		<%= form.submit %>
	</p>
<% end %>
<%= link_to 'Back', clubs_path %>	
